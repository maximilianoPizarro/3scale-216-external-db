apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
labels:
  - pairs:
      app: system-postgresql-external
    includeSelectors: false
resources:
  - serviceaccount.yaml
  - pvc.yaml
  - service.yaml
  - deployment.yaml
