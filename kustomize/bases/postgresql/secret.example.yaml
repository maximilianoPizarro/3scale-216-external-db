# Example only — do not commit real passwords. Copy into 3scale-db before PostgreSQL 15 starts.
apiVersion: v1
kind: Secret
metadata:
  name: system-database
  namespace: 3scale-db
type: Opaque
stringData:
  DB_USER: system
  DB_PASSWORD: CHANGE_ME
