apiVersion: v1
kind: Service
metadata:
  name: system-redis-service
  labels:
    app: system-redis-external
spec:
  type: ClusterIP
  ports:
    - port: 6379
      targetPort: 6379
      protocol: TCP
  selector:
    deployment: system-redis-external
